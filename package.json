{
  "name": "tron-github-action",
  "version": "1.0.0",
  "description": "Generate Tron light cycle animations from GitHub contribution data",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt",
    "package": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github-action",
    "tron",
    "animation",
    "svg"
  ],
  "author": "markpython86",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
} 